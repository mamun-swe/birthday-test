import { dashboard } from 'src/services/admin/dashboard.service';
import { user } from 'src/services/admin/user.service';
import { role } from 'src/services/admin/role.service';

export const admin = {
  dashboard,
  user,
  role,
};
