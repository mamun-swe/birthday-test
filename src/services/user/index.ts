import { dashboard } from 'src/services/user/dashboard.service';
import { contact } from 'src/services/user/contact.service';
import { profile } from 'src/services/user/profile.service';
import { settings } from 'src/services/user/settings.service';

export const user = {
  dashboard,
  contact,
  profile,
  settings,
};
